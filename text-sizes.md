# أحجام النص — Text Sizes

عدل القيم اللي تبغاها وأقولك طبق.

---

## Hero
| العنصر | الحجم الحالي |
|---|---|
| badge (مصمم ومطور منتجات) | `12px` font-mono font-semibold |
| h1 (أنا دانيال…) | `clamp(2.5rem,6vw,4.5rem)` = 40px–72px |
| tagline (بفضل خلفية…) | `clamp(1rem,1.5vw,1.0625rem)` = 16px–17px |
| buttons (مشاريعي, تواصل) | `14px` (text-sm) |

## About
| العنصر | الحجم الحالي |
|---|---|
| label (نبذة عني) | `12px` font-mono font-semibold |
| h2 (عن نفسي) | `clamp(1.5rem,3.5vw,2.25rem)` = 24px–36px |
| body (أقود فريق…) | `17px` |

## Experience
| العنصر | الحجم الحالي |
|---|---|
| label (خبرات) | `12px` font-mono font-semibold |
| h2 (خبرات مهنية) | `clamp(1.5rem,3.5vw,2.25rem)` = 24px–36px |
| date (٢٠٢٤ — حتى الآن) | `12px` (text-xs) font-mono font-semibold |
| role (مصمم منتجات) | `18px` (text-lg) font-semibold |
| company (شركة كذا) | `14px` (text-sm) |
| description | `14px` (text-sm) |

## Skills
| العنصر | الحجم الحالي |
|---|---|
| label (الأدوات والخبرات) | `12px` font-mono font-semibold |
| h2 (المهارات) | `clamp(1.5rem,3.5vw,2.25rem)` = 24px–36px |
| skill tags (Figma, React…) | `14px` (text-sm) |

## Projects
| العنصر | الحجم الحالي |
|---|---|
| label (أعمالي) | `12px` font-mono font-semibold |
| h2 (مشاريعي) | `clamp(1.5rem,3.5vw,2.25rem)` = 24px–36px |
| filter buttons (الكل, ويب…) | `12px` font-mono font-semibold |
| project title | `16px` (text-base) font-semibold |
| project description | `13px` |
| tech tags (React, API…) | `12px` font-mono font-semibold |

## Contact
| العنصر | الحجم الحالي |
|---|---|
| label (تواصل) | `12px` font-mono font-semibold |
| h2 (هل لديك مشروع؟) | `clamp(1.5rem,3.5vw,2.25rem)` = 24px–36px |
| desc (أنا متاح…) | `16px` (text-base) |
| button (hello@daniel.com) | `14px` (text-sm) |

## Footer
| العنصر | الحجم الحالي |
|---|---|
| text | `12px` (text-xs) |

## Nav
| العنصر | الحجم الحالي |
|---|---|
| links normal | `14px` (text-sm) |
| links scrolled | `12px` (text-xs) |
| lang buttons (EN/AR) | `12px` (text-xs) |
| mobile drawer links | `14px` (text-sm) |
